{
  "name": "web-tools",
  "private": true,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/validate-site.mjs",
    "test": "node --test tests/*.test.mjs apps/log/log_manager.test.js"
  }
}
